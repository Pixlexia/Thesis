package game;

/*
 *  Robot actions
 */

public enum RAction {
	moveLeft, moveRight, interact, blueSlot, redSlot, yellowSlot,
	add, subtract, multiply, divide, equals, modulo, integer,
	startIf, endIf, startElse, endElse, isEqual, isNotEqual, isGreaterThan, isLessThan,
	startLoop, func1, func2, endLoop
}